name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run baseline checks
        run: make ci
