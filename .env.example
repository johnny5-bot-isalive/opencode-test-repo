# Environment variables for opencode-test-repo
# Copy this to .env and fill in values

# Project namespace (e.g. my-app, api-service)
PROJECT_NAME=opencode-test-repo

# Environment (development, staging, production)
NODE_ENV=development